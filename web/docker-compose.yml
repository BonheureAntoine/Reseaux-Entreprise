version: "0.1"
services:
  web:
    build: ./website
    container_name: app
    ports:
      - "3000:3000"
    restart: always
    networks:
      default:

networks:
  default:
    ipam:
      driver: default
      config:
        - subnet: 192.168.0.0/24
          gateway: 192.168.0.1
